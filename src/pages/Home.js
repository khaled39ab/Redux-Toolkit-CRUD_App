import React from 'react';

const Home = () => {
    return (
        <div style={{textAlign: 'center'}}>
            <h1>Welcome to Home page</h1>
            <p>This a simple CRUD operation app to create a library book list. Some features are add new book, edit existing book and delete existing book</p>
        </div>
    );
};

export default Home;